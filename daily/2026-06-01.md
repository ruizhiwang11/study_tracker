# 2026-06-01

## Studied
- Set up study tracking repo structure
- Reviewed existing work: ring buffer (my_cpp/ringbuffer/), overload set / perfect forwarding, OS boot (my_os/)

## Key insight
- Already have working ring buffer and perfect forwarding code — need to annotate these with what they exercise (lock-free design, universal ref, SFINAE)

## Code practiced
- `my_cpp/ringbuffer/buffer.cpp` — ring buffer
- `my_cpp/overload_Set/perfect_fowarding.cpp` — forwarding refs
- `my_os/` — minimal boot / RISC-V via opensbi

## Weak points
- Need to audit ring buffer for memory ordering (which std::memory_order are used and why)
- x86 register roles not fully locked in (rax/rbx/rsp/rip/rsi/rdi)

## Session 2 — Overload Resolution
- Full 3-phase process: name lookup (unqualified + ADL) → resolution → access check
- ICS ranking: exact match > qualification > promotion > conversion > user-defined > ellipsis
- Lvalue/rvalue reference overloading + reference collapsing rules
- Concepts partial ordering and subsumption — most constrained wins
- Lambda wrapper pattern: why overloaded sets can't be passed as template args directly
- See `my_cpp/overload_Set/Callable.cpp` and `test.cpp`

## Key insight (session 2)
- Overloaded function names have no single address — compiler resolves at call site.
  Generic lambda defers resolution to inside the body where arg types are known.
- Concepts don't bypass overload resolution — they add a partial order on top of it.

## Tomorrow
- Audit ring buffer memory_order choices
- Review x86 calling convention (System V AMD64 ABI)
- Continue NJU OS material
